System.setProperty("webdriver.chrome.driver","C:\\Users\\deepti.srivastava\\chromedriver.exe");
		WebDriver driver=new ChromeDriver();
		
		driver.get("https://www.tripadvisor.in/"); //Hit url on browser
		
		System.out.println(driver.getCurrentUrl());// validate
		//driver.findElement(By.linkText("Flights")).click();		
driver.findElement(By.xpath("//*[@id='global-nav-flights']")).click();
		driver.findElement(By.linkText("One-way")).click();
driver.findElement(By.xpath("//div[@id='ow_ui_picker']")).click();
		
		List<WebElement> dates=driver.findElements(By.cssSelector("span[class*='dsdc-cell']"));
		
		int count=driver.findElements(By.cssSelector("span[class*='dsdc-cell']")).size();
		for(int i=0;i<count;i++)
		{
			String text=driver.findElements(By.cssSelector("span[class*='dsdc-cell']")).get(i).getText();
			if(text.equalsIgnoreCase("25"))
					{
				driver.findElements(By.cssSelector("span[class*='dsdc-cell']")).get(i).click();
				break;
}
		}
		
		driver.manage().window().maximize();
		
		//driver.findElement(By.className("submit_text")).click();
		driver.findElement(By.xpath("//div[@class='ui_column is-mobile submit_wrap']//button[@class='form_submit searchBtn en_IN en']//span[@class='submit_text']"));
		
		//driver.close();
		
	
		

	}

}
